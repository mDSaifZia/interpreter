/// This is a multi-line comment
/// spanning multiple lines.
/// End of multi-line comment ///

var x = 10;
var y = 20;

fn add(a, b) {
    return a + b;
}

var result = add(x, y);

loop i from(1, 5) {
    // Double the value of i
    var doubled = i * 2;}

if (result > 25) {
    var status = "high";
} else if (result == 25) {
    var status = "equal";
} else {
    var status = "low";
}

while (x < y) {
    x = x + 1;
}
